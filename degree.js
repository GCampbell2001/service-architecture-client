let courseList = [1, 2, 3, 4, 5];

function signout() {
    location.replace("index.html");
}

function generateTable() {
    var table = document.getElementById("courseTable");
    var rowCount = table.rows.length;
    var row = table.insertRow(rowCount);

    
}